

document.addEventListener('DOMContentLoaded', function () {
  
  function save(tagnum) {
  }
  
  document.getElementById('porntag').addEventListener('click', save(1));
  document.getElementById('adulttag').addEventListener('click', save(2));
  
});